# Vous etes libre d'implementer la fonction my_mean comme vous le souhaiter, tant qu'elle retourne la moyenne de tous les nombres contenus dans le vecteur `my_vector`.
#
# Astuce #1: sum() retourne la somme d'un vecteur.
# 	Ex: sum(c(1, 2, 3)) retournera 6
#
# Astuce #2: length() retourne la taille d'un vecteur.
# 	Ex: length(c(1, 2, 3)) retournera 3
#
# Astuce #3: je ne vous apprends normalement rien mais la moyenne de tous les nombres d'un vecteur est egale a la somme des nombres du vecteur divisee par la taille du vecteur.
#
# Note pour celles et ceux qui se sentiraient plus malins que les autres: n'utilisez pas la fonction 'mean()', nous essayons de vous apprendre quelque chose ici, jouez le jeu !
#
# Soyez vigilent a bien sauvegarder votre script avant de valider vos changements en tapant submit() dans la console.
my_mean = function(my_vector) {
  # ecrivez votre code ici
  # Rappelez-vous, la derniere expression evaluee est celle retournee par la fonction!
}
